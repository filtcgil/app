dependencyResolutionManagement {
    // Cambia FAIL_ON_PROJECT_REPOS in PREFER_SETTINGS o rimuovi il blocco
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) 
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "UnionHubApp"
include(":app")
