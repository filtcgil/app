pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) // Cambiato qui
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "UnionHubApp"
include(":app")
